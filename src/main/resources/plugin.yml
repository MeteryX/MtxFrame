name: MtxFrame
version: '${project.version}'
main: at.mtxframe.mtxframe.MtxFrame
api-version: '1.20'
load: STARTUP
author: MeteryX
depend:
  - PlotSquared

commands:
  itemtest:
    usage: /itemtest
    aliases:
      - citem
